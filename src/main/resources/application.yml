#APACHE
server:
  port: 8069

#REFRESH_SCOPE CONFIG
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/